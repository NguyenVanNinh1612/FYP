@model IEnumerable<WebBook.Models.Menu>

@if (Model != null && Model.Any())
{
	<ul class="navbar_menu">
		@foreach (var item in Model)
		{
			<li><a asp-controller="Product" asp-action="Index" asp-route-slug="@item.Slug">@item.Name</a></li>
		}
	</ul>
}